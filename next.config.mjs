/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['kangacook.com'], // Add the external hostname here
      },
    output: 'export',
};

export default nextConfig;
